# 변경 이력

## 2026-04-16

### 문서화
- README.md 추가: 프로젝트 전체 개요 및 사용 가이드
- DESIGN.md 추가: 디자인 시스템 문서화 (색상, 타이포그래피, 컴포넌트 스타일)

### 서비스명 통일 및 더미 데이터 추가
- 사이드바 타이틀: 목표관리 시스템 → 차일디 목표관리 시스템
- 브라우저 탭 타이틀: 목표관리 시스템
- 로그인 화면 타이틀: 차일디 목표관리 시스템
- 사용자 추가: 이개발 → 박성공
- 김전략 OKR 1개 추가 (데이터 분석 역량 강화)
- 박성공 OKR 4개 추가 (고객 만족도, 마케팅, 운영 자동화, 팀 협업)
- 요청 관리에 승인 대기 건 2개 추가 (박성공)

### UI 레이아웃 통일
- 대시보드 UI 통일: 탭과 기간 선택 레이아웃을 다른 화면들과 동일하게 변경
- 목표 설정 및 합의: 기간 선택 드롭다운 위치 정렬 수정
- 4개 화면(대시보드, 목표 설정, 내 목표 관리, 요청 관리) 모두 동일한 레이아웃 적용

### UI 스타일 통일화

**변경 내용:**
- '요청 관리' 페이지의 UI 스타일을 '내 목표 관리' 페이지와 동일하게 통일

**상세 변경사항:**

1. **기간 선택 드롭다운 스타일 변경**
   - 기존: 흰색 배경 컨테이너 내부에 위치, `bg-surface-container-low` 사용
   - 변경: 단독 배치, `bg-surface-container` 사용
   - 패딩 조정: `px-4 py-2` → `px-3 py-1.5`

2. **테이블 헤더 스타일 통일**
   - 배경색: `bg-surface-container-low` → `bg-surface-container`
   - 텍스트 스타일: `uppercase tracking-widest` 제거
   - 패딩 조정: `py-5` → `py-4`
   - 테두리 스타일: `border-blue-100` → `border-blue-50`

3. **테이블 컨테이너 스타일 통일**
   - 테두리: `border-blue-100` → `border-blue-50`
   - 하단 여백 제거: `mb-10` 삭제

4. **레이아웃 간소화**
   - 기간 선택 영역의 불필요한 래퍼 제거
   - "총 N건의 결재 라인" 카운터 표시 제거

**영향 범위:**
- `script.js` 파일의 `renderRequests()` 함수
- 필드명 및 기능은 변경 없음 (스타일만 수정)

**목적:**
- 전체 애플리케이션의 일관된 사용자 경험 제공
- 페이지 간 시각적 통일성 확보

